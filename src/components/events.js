import React from 'react';
import TabPage from './tab_page';

class Events extends TabPage {
  render () {
    return (
      <section className="tab-page">
        <h2>Events</h2>
      </section>
    );
  }
}

export default Events;
